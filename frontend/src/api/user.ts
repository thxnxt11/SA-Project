// import axios from "axios";

// const API_URL = "http://localhost:8000";

// const token = localStorage.getItem("token"); // JWT token หลัง login

// export const api = axios.create({
//   baseURL: API_URL,
//   headers: {
//     Authorization: `Bearer ${token}`,
//     "Content-Type": "application/json",
//   },
// });

// export const getDepartments = () => api.get("/departments");
// export const createDepartment = (name: string) => api.post("/departments", { name });

// export const getPositions = () => api.get("/positions");
// export const createPosition = (name: string) => api.post("/positions", { name });

// export const createStaff = (data: unknown) => api.post("/staff", data);
